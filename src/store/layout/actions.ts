import { ActionTree } from 'vuex';
import { StateInterface } from '../index';
import { ILayoutState } from './state';

const actions: ActionTree<ILayoutState, StateInterface> = {
  someAction(/* context */) {
    // your code
  },
};

export default actions;
